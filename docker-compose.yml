services:
  bot:
    build: .
    container_name: polyedge_bot
    init: true
    restart: unless-stopped
    stop_signal: SIGTERM
    stop_grace_period: 30s
    env_file:
      - ./config/.env
    volumes:
      - ./logs:/app/logs
      - ./config:/app/config:ro
