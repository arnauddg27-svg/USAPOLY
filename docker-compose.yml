version: "3.8"
services:
  bot:
    build: .
    container_name: polyedge_bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
